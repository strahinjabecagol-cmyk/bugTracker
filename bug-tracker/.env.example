# Transport: set to "http" for HTTP mode, unset or omit for stdio (default)
MCP_TRANSPORT=http

# Port for HTTP transport (default: 3001)
PORT=3001

# API key for Bearer token auth — required when MCP_TRANSPORT=http
MCP_API_KEY=your-secret-key-here
